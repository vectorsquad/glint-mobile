import 'package:flutter/material.dart';

import '../scripts/util.dart';

class DeckInfo extends StatelessWidget {
  final HttpProps deckProps;

  const DeckInfo(this.deckProps, {super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("lmao");
  }

}